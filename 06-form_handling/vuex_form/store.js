const state = {};

const mutations = {};

const actions = {};

const getters = {};

// globalize the store with window.store for it to be accessed elsewhere
window.store = Vuex.createStore({
  state,
  mutations,
  actions,
  getters,
});
