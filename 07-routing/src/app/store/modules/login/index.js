import axios from "axios";

const state = {};
const mutations = {};
const actions = {};
const getters = {};

const loginModule = {
  state,
  mutations,
  actions,
  getters,
};

export default loginModule;
